stages:
  - lint

pre-commit:
  stage: lint
  image: python:3.11-alpine
  before_script:
    - pip install pre-commit
    - pre-commit install
  script:
    - pre-commit run --all-files --config .pre-commit-ci.yaml
  allow_failure: true
