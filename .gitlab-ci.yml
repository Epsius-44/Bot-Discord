stages:
  - lint

pre-commit:
  stage: lint
  image: python:3
  before_script:
    - pip install pre-commit
    - pre-commit install
    - npm install -g @typescript-eslint/eslint-plugin @typescript-eslint/parser eslint-config-prettier eslint
  script:
    - pre-commit run --all-files
